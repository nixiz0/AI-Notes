if (window.innerWidth > 699) {
    // The screen is wider than 699 pixels, so load the desktop page
    window.location.href = "index_desktop.html";
} else {
    // The screen is smaller than 699 pixels, so load the mobile page
    window.location.href = "index_mobile.html";
}